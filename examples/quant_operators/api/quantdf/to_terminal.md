---
type: atom
id: api/quantdf/to_terminal
summary: 'QuantDF.to_terminal: 将结果 dataframe 渲染为适合终端阅读的字符串'
status: active
version: 1
tags: ["api-doc", "quantdf", "output"]
intensity: 5
maturity: draft
imports:
  required: ["api/quantdf"]
  recommended: ["api/quantexpr"]
  related: ["api/quantdf/to_layout"]
---
# QuantDF.to_terminal

## 签名

```
QuantDF.to_terminal(*[, layout])
```

## 说明

将结果 dataframe 渲染为适合终端阅读的字符串

## 所属类

[QuantDF](Quant DataFrame — 在 DataFrame 级别进行截面/时序/滚动操作。底层计算委托给 QuantExpr。)
